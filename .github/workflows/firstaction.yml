name: Hello Actions

# on:
#   schedule:
#   - cron: "0 2 * * 1-5"
on:
  push:
    branches:
      - master
      - build
  pull_request:
    types: [closed]

jobs:
  build:
  
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Hello, World! Time to eat! 🍕
